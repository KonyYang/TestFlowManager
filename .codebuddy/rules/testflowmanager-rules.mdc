---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-11T09:39:52.812Z
provider: 
---

# TestFlowManager 项目规范

## 1. 架构原则

### 1.1 主窗口职责边界
主窗口 (MainWindow) 是**应用壳层**，不是业务杂糅层：
- ✅ 页面容器、导航与菜单、顶层状态展示、项目会话切换、全局生命周期管理
- ❌ Matrix 内部表格同步、导入导出实现细节、feature 内部事件处理

### 1.2 Matrix 核心定位
- Matrix 是系统核心工作区（默认首页）
- **禁止**将 Matrix 内部实现（table/toolbar/managers/handlers/sync logic）直接揉进主窗口
- 目标：`MainWindow` → `MatrixWorkspace` → 内部组件

### 1.3 渐进式重构
- 不做一次性大重写
- 优先：兼容层、页面级抽离、上下文对象引入、单点入口收口
- **先边界，后内部**：先理清主窗口与 Matrix 边界，再拆 Matrix 内部服务

---

## 2. 代码结构规范

### 2.1 目录结构
```
src/features/main_window/
├── view/           # UI 定义
├── controller/     # 页面控制器
├── components/     # UI 组件（样式无关）
├── constants/      # 样式常量
├── integration/    # 功能集成
└── handlers/       # 事件处理器
```

### 2.2 命名规范
| 类型 | 规范 | 示例 |
|------|------|------|
| 类 | PascalCase | `MatrixService`, `ProjectContext` |
| 方法/函数 | snake_case | `create_sidebar()`, `update_matrix()` |
| 常量 | UPPER_SNAKE | `LIMS_APP_STYLESHEET`, `DEFAULT_TIMEOUT` |
| 私有属性 | `_leading_underscore` | `_matrix_service`, `_current_project` |

### 2.3 文件组织
- 单个 Python 文件不超过 **500行**（GUI 文件可适当放宽至 800行）
- 超过则拆分：UI 组件抽离到 `components/`，样式抽离到 `constants/`
- 避免无意义的中间层，只为复用而抽象

---

## 3. PyQt5 编码规范

### 3.1 UI 组件拆分模式
```python
# components/header_components.py
class HeaderComponents:
    def __init__(self, parent):
        self._parent = parent
    
    def create_app_header(self) -> QWidget:
        # 创建并返回顶栏 widget
        pass

# main_window_ui.py
from .components.header_components import HeaderComponents

class MainWindow:
    def _setup_basic_ui(self):
        self._header_components = HeaderComponents(self)
        self._app_header = self._header_components.create_app_header()
```

### 3.2 信号与槽
- 优先使用 `pyqtSignal` 而非回调
- 避免在 UI 层直接处理业务逻辑

### 3.3 日志替代 print
```python
# ❌ 禁止
print(f"Project opened: {path}")

# ✅ 使用 logger
logger.info(f"Project opened: {path}")
logger.debug(f"Matrix data shape: {rows}x{cols}")
```

---

## 4. COM 与资源管理

### 4.1 必须遵守
- 不破坏 COM 资源释放路径
- Word/Excel 操作必须 `quit()` 释放
- 应用退出时确保清理逻辑完整

### 4.2 涉及高审慎级文件
- `src/utils/word_utils.py`
- `src/utils/excel_utils.py`
- `src/utils/email_utils.py`
- `src/app/application.py`

---

## 5. 高风险业务行为

以下流程在重构中**必须保行为一致**：
- 打开项目 / 新建项目
- 自动导入 `matrix.xlsx`
- Matrix 编辑 / 导出
- LTR 申请处理
- 报告生成 / 更新 / 客户版转换

---

## 6. 上下文与状态

### 6.1 ProjectContext 规范
```python
@dataclass
class ProjectContext:
    project_path: Path
    dl_number: str
    application_data_path: Path
    matrix_file_path: Path
```

### 6.2 约束
- 全局状态只存真正的全局上下文
- 事件只表达一次性动作
- 同一业务动作**不同时**依赖重复事件和重复状态

---

## 7. 重构交付标准

| 检查项 | 要求 |
|--------|------|
| pytest | 相关测试通过 |
| py_compile | 无语法错误 |
| 功能 | 关键回归路径验证通过 |
| 文档 | 结构性改动同步更新 `docs/` |

---

## 8. 禁止的模式

- ❌ 主窗口直接持有 `self._matrix_table`、`self._matrix_sync_handler`
- ❌ `巨型 controller` / `巨型 service`
- ❌ 依赖隐式全局状态完成正常业务流程
- ❌ 无意义抽象层
- ❌ print 语句（生产代码）